name: Build and register the ami

on:
  push:
    branches: [ main ]

jobs:
  buildAndRegisterAmi:
    runs-on: ubuntu-latest

    steps:
    - name: build and register Ami
      run: 
        pwd
        # ./buildAmi.sh